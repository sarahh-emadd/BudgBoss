// lib/core/constants/colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (from your screenshots - the blue)
  static const primaryColor = Color(0xFF3737CD);
  static const primaryLightColor = Color(0xFF5454E9);
  
  // Background colors
  static const backgroundColor = Colors.white;
  
  // Text colors
  static const textDark = Color(0xFF1E1E1E);
  static const textLight = Colors.white;
  
  // Indicator colors
  static const indicatorActive = primaryColor;
  static const indicatorInactive = Color(0xFFD9D9D9);
}